# 信道估计与 CIR 处理

> 本文整理自一次关于 5G NR 物理层信道估计流程的系统性讨论，涵盖 CIR Length 盲估计、循环相关定理、h[l] 提取与降噪、以及 NTN 卫星场景的特殊处理逻辑。

---

## 1. 核心概念：CIR Length 的物理意义

### 1.1 什么是 CIR Length

冲激响应长度 $L$ 是指：系统在任意时刻 $n$ 的输出 $y[n]$，会受到**当前时刻以及之前 $L-1$ 个时刻**的输入信号的共同影响。

用卷积公式表达：
$$ y[n] = \sum_{k=0}^{L-1} h[k] \cdot x[n-k] $$

$y[n]$ 由 $x[n]$（当前输入）、$x[n-1]$（前一个输入）、……、$x[n-L+1]$（前 $L-1$ 个输入）加权叠加而成。$L$ 本质上是系统的**"记忆深度"**。

### 1.2 OFDM 场景下的物理意义

在基站到 UE 的无线信道中，$L$ 的物理意义是**最大多径时延对应的抽头数**。假设 $L=5$：

- 基站此刻发射的符号 $x[n]$，UE 不仅会在当前时刻收到（直射路径），还会在接下来 4 个采样时刻陆续收到它的"回声"（反射路径）。
- 反过来说，UE 在时刻 $n$ 收到的信号 $y[n]$，是基站最近 5 个符号的叠加——当前 $x[n]$ 加上前 4 个符号的多径拖尾。

$L$ 越大，**符号间干扰（ISI）**越严重。循环前缀（CP）的设计目的，就是给这个"拖尾"留出缓冲空间，保证圆周卷积的等价性不被破坏。

### 1.3 理想单径场景

在仪表直连的理想单径场景下，$L=1$，卷积退化为：
$$ y[n] = h[0] \cdot x[n] $$
当前输出只取决于当前输入，系统**没有记忆**，没有多径拖尾，没有 ISI。这就是 AWGN 信道的本质——纯衰减加相移的复标量乘法器。

### 1.4 两种等价表述

- **表述 A**："一次输入影响后面 $L$ 次输出"（发射端视角）
- **表述 B**："当前输出由最近 $L$ 次输入共同决定"（接收端视角）

两者在数学上等价（卷积交换律），但从物理层开发角度，**表述 B 更贴合 UE 接收机视角**——我们关心的是"此刻收到的信号里混进了多少历史符号的干扰"。

---

## 2. 因果性与时间对齐

### 2.1 "当前时刻"的定义

物理层算法中的"当前时刻 $n$"，是 UE 接收机通过同步算法，以**最早到达的首径（First Path）**为锚点强行定义的相对时间零点。

- **首径（First Path）**：被对齐到 $h[0]$ 的位置。
- **后续多径**：根据它们比首径晚到了多少个采样周期，依次落在 $h[1], h[2], \ldots, h[L-1]$ 上。

### 2.2 物理层面：绝对时延与相对时延

真实无线传播有绝对传播时间，但基带处理只关心**多径之间的相对时延差**。UE 的同步模块（PSS/SSS 搜索、定时同步）不断调整 FFT 窗位置，找到信号能量最早到达的时刻，定义为 $n=0$。

### 2.3 数学层面：因果系统的强制对齐

物理可实现的无线信道必须是**因果系统**——$t$ 时刻的输出不能依赖 $t$ 之后的输入。当信道写成 $h = [h_0, h_1, \ldots, h_{L-1}]^T$ 时，已在数学上强制把最早到达的信号"平移"到索引 0，确保因果性。

### 2.4 工程层面：同步偏差

- **同步超前**：FFT 窗比首径早开启 → 首径落在 $h[1]$ 或 $h[2]$，$h[0]$ 为纯噪声。可接受，只是浪费抽头。
- **同步滞后（更危险）**：FFT 窗比首径晚开启 → 首径能量"溢出"到上一个符号尾部，破坏圆周卷积假设，导致严重 ISI。

---

## 3. CIR Length 盲估计算法

核心任务：在不知道真实 $L$ 的前提下，从含噪的 $N$ 维 CIR 估计向量 $\hat{h}_{LS}$ 中，自动划出分界线——线左边是信号+噪声，线右边是纯噪声。

### 3.1 固定门限法（最简单）

设定功率门限 $\gamma$，从尾部往前扫，找到最后一个幅度超过 $\gamma$ 的抽头：
$$ \hat{L} = \max\{ i \mid |h[i]|^2 > \gamma \} $$

**工程细节：**
- 门限通常取 $\gamma = \alpha \cdot \hat{\sigma}^2$，$\alpha$ 取 3~6（5~8 dB）。
- 噪声方差 $\hat{\sigma}^2$ 通过 CIR 尾部（最后 10% 抽头）的平均功率估计。
- **反向搜索（Reverse Detection）**：从尾部往前扫，避免中间噪声尖峰被误判。

**优点**：计算量极小，适合 FPGA 实时流水线。
**缺点**：低 SNR 下容易误判。

### 3.2 基于统计特性的辅助函数法（经典学术方案）

构造辅助函数 $J(l)$，遍历所有可能的 $l$ 值，找到使信号区与噪声区差异最大的分割点：
$$ J(l) = \frac{\sum_{i=0}^{l-1} |h[i]|^2}{l} - \beta \cdot \frac{\sum_{i=l}^{N-1} |h[i]|^2}{N-l} $$

**优点**：自适应能力强，0 dB SNR 下仍可工作。
**缺点**：需遍历所有 $l$，计算量较大。

### 3.3 基于压缩感知的稀疏重构（前沿方向）

将 CIR 估计建模为稀疏信号恢复问题，通过 $\ell_1$ 范数最小化或迭代硬阈值（IHT）直接从含噪观测中恢复稀疏 CIR 向量。

**优点**：极低 SNR 和超稀疏场景（毫米波、NTN）下性能最优。
**缺点**：计算复杂度最高，目前主要停留在研究阶段。

### 3.4 工程实践中的混合策略

1. **粗估计**：固定门限法快速得到初始 $\hat{L}_0$。
2. **精估计**：在 $\hat{L}_0 \pm 5$ 窗口内用辅助函数法精细搜索。
3. **平滑跟踪**：对连续多个 OFDM 符号的 $\hat{L}$ 做滑动平均或一阶 IIR 滤波，避免剧烈跳变。

---

## 4. 循环相关定理

### 4.1 数学本质

循环相关定理建立了时域与频域的等价关系：
$$ \text{DFT}\{ r[n] \star p[n] \} = R[k] \cdot P^*[k] $$

时域的循环互相关，在频域变成了两个复数向量的**逐元素相乘（Hadamard Product）**。

### 4.2 "循环"的含义

DFT 隐含地把有限长序列看作无限周期序列的一个周期：
- **线性相关**：移位后补 0。
- **循环相关**：移位后从头部绕回来（Wrap-around）。

这与 OFDM 的**循环前缀（CP）**机制完美契合——CP 使线性卷积转化为圆周卷积，因此可直接使用循环相关定理。

### 4.3 工程加速效果

| 方法 | 计算复杂度 | $N=1024$ 时运算量 |
|------|-----------|------------------|
| 时域直接计算 | $O(N^2)$ | ~100 万次 |
| 频域 FFT 加速 | $O(N \log N)$ | ~3 万次 |

频域实现步骤：
1. $\text{FFT}(r) \to R[k]$
2. $\text{FFT}(p) \to P[k]$，取共轭 $P^*[k]$
3. 逐点相乘 $Y[k] = R[k] \cdot P^*[k]$
4. $\text{IFFT}(Y) \to y[n]$（即所有偏移量 $\tau$ 的相关值）

效率提升 **30 倍以上**。

---

## 5. 获取 $h[l]$：降噪与提取

得知 CIR 长度 $\hat{L}$ 后，从含噪估计中提取有效信道系数 $h[l]$。

### 5.1 方案一：时域直接截断

$$ h[l] = \hat{h}_{LS}[l], \quad l = 0, 1, \ldots, \hat{L}-1 $$
$$ h[l] = 0, \quad l \geq \hat{L} $$

**优点**：计算量几乎为零。
**缺点**：$\hat{L}$ 不准时，要么残留噪声，要么丢失弱多径。

### 5.2 方案二：LS + DFT 降噪（工程主流）

利用时域稀疏性抑制噪声，完整流程：

1. **频域 LS 粗估计**：$\hat{H}_p = Y_p / X_p$
2. **频域插值**：填满所有子载波，得到 $\hat{H}_{full}$
3. **IFFT 变换到时域**：$\hat{h}_{time} = \text{IFFT}\{\hat{H}_{full}\}$
4. **时域加窗/截断**：$\hat{h}_{win}[l] = \hat{h}_{time}[l] \cdot w[l]$
   - 硬截断（矩形窗）：前 $\hat{L}$ 个点为 1，后面全 0
   - 平滑窗（升余弦窗）：过渡带逐渐衰减，避免频谱泄露
5. **FFT 变回频域**：$\hat{H}_{clean} = \text{FFT}\{\hat{h}_{win}\}$

**原理**：噪声在时域均匀分布在整个 $N_{FFT}$ 上，信号集中在前 $\hat{L}$ 个抽头。截断相当于低通滤波，大幅提升 SNR。

---

## 6. 信道估计 vs 信道均衡：独立步骤

### 6.1 信道估计——"诊断"

- **任务**：利用已知导频（DMRS/CRS），推算信道当前状态，输出 $H[k]$。
- **本质**：获取信道状态信息（CSI），为后续处理提供"地图"。

### 6.2 信道均衡——"治疗"

- **任务**：利用估计出的 $H[k]$，对接收数据符号 $R[k]$ 进行补偿。
- **本质**：求信道的"逆"，恢复原始发送符号 $X[k]$。
- **迫零均衡（ZF）**：$\hat{X}[k] = R[k] / H[k]$
- **MMSE 均衡**：考虑噪声功率，避免深衰落处过度放大噪声。

### 6.3 关键认知

$h[l]$ 是时域表达，但**不直接和时域接收信号相乘**。OFDM 接收端去 CP 后立刻做 FFT 转到频域，在频域上做逐点补偿。

---

## 7. 完整接收机流水线
接收时域信号 r[n]  
→ 时频同步（PSS/SSS 检测）  
→ 去 CP + FFT（时域 → 频域）  
→ 提取导频（DMRS/CRS）  
→ 频域 LS 粗估计（得到 Ĥ_p）  
→ 频域插值（填满所有子载波，得到 Ĥ_full）  
→ IFFT 变换到时域（得到含噪的 ĥ_time）  
→ 👉 CIR Length 估计（找出有效边界 L̂）  
→ 时域加窗/截断（利用 L̂ 滤除噪声，得到 ĥ_win）  
→ FFT 变回频域（得到最终的 Ĥ_clean）  
→ 信道均衡（用 Ĥ_clean 补偿数据符号）  
→ 解调译码


CIR Length 估计夹在"时域变换"和"时域降噪"之间，起承上启下作用——它决定了降噪窗口的宽度，直接影响最终信道估计精度。

---

## 8. NTN 卫星场景的特殊性

### 8.1 多径结构差异

| 特性 | 地面蜂窝 | NTN 卫星 |
|------|---------|---------|
| 主导路径 | NLOS 多径丰富 | LOS 绝对主导 |
| NLOS 径数量 | 十几~二十几根 | 最多 2~3 根 |
| 时延扩展 | 较大 | 极小（几百 ns 以内）|
| CIR 长度 $L$ | 较大 | 非常短 |

### 8.2 CIR Length 估计的反直觉现象

NTN 的 CIR 非常"瘦"，有效能量几乎全部集中在 LOS 主径上：
- $\hat{L}$ 估大 → 多保留几个噪声抽头，噪声占比急剧上升，SNR 严重恶化。
- $\hat{L}$ 估小 → 直接丢掉 LOS 主径，链路中断。

因此需要**更高的门限灵敏度**和**更精细的反向搜索策略**，通常结合卫星星历信息做辅助判决。

### 8.3 高动态多普勒挑战

- LEO 卫星速度约 **7.5 km/s**
- S 频段多普勒频移可达 **±48 kHz**，Ka 频段可达 **±500 kHz**
- 远超地面高铁场景的 ~1 kHz
- 会破坏 OFDM 子载波正交性，引入严重 ICI

必须先做**多普勒预补偿**（利用 GNSS + 星历），把残余频偏降到可接受范围后，才能进行常规 CIR 估计和均衡。

### 8.4 NTN 信道估计流水线
GNSS 定位 + 星历解析  
→ 多普勒预补偿（开环粗补偿）  
→ 时频同步（SSB 检测，残余频偏估计）  
→ 去 CP + FFT  
→ 提取导频（DMRS）  
→ 频域 LS 粗估计  
→ 频域插值  
→ IFFT 变换到时域  
→ 👉 CIR Length 估计（高精度门限 + 星历辅助）  
→ 时域加窗/截断  
→ FFT 变回频域  
→ 残余频偏校正 + 信道均衡

  
--- 
## 标签 
#5G-NR #物理层 #信道估计 #CIR #OFDM #NTN #多普勒 #均衡