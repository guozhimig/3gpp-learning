# Obsidian 使用技巧

## 基本概念
1. **仓库（Vault）**：每一个仓库就是一个独立的笔记空间，对应你硬盘上的一个文件夹。
2. **插件是跟着仓库走的**：插件配置保存在仓库内部（`.obsidian` 目录），
   - 换了仓库，相当于换了一个“全新”的 Obsidian 环境；
   - **插件需要在新仓库里重新启用 / 安装**（同步工具只同步文件的话，要注意 `.obsidian` 是否一并同步）。
   - 如果你看到类似 `\.obsidian\plugins\obsidian-git\` 这样的目录：  
     - **把整个目录拷贝到新仓库的 `\.obsidian\plugins\` 下，就等于把插件“安装过去”了（连同配置 `data.json` 一起）**；  
     - 但 Obsidian 里这个插件是否“启用”，还取决于 `\.obsidian\community-plugins.json`，  
       - 只复制插件目录 → 通常需要在“设置 → 社区插件”里手动再把插件开关打开；  
       - 如果你把 `community-plugins.json` 也一并同步，就等于“插件 + 启用状态”一起同步到新仓库。
3. **Markdown 为核心**：所有笔记本质就是本地的 `.md` 文件，不依赖云服务，迁移成本低。
4. **双向链接**：`[[笔记名]]` 可以把两篇笔记互相连起来，形成“知识网络”而不是“树状目录”。
5. **标签（#tag）**：适合做跨文件的分类、状态标记，比如 `#todo`、`#idea`、`#project/blueWave`。

---

## 基本操作习惯
1. **新建笔记**
   - `Ctrl + N`：快速创建新笔记；
   - 推荐统一命名规则，例如：`2026-02-10 日记`、`项目-功能名`，便于搜索。
2. **快速打开笔记**
   - `Ctrl + O`：输入关键字模糊搜索并打开笔记；
   - 可以只记笔记名的一部分，搜索性能很强。
3. **拆分视图 / 多窗口**
   - 右上角“拆分”按钮或快捷键打开左右两栏；
   - 一边看资料，一边写笔记，非常适合学习和写文档。

---

## Markdown 基本语法速查（Obsidian 里常用）

> 所有示例都可以直接复制到 Obsidian 里试。

### 标题
   - 语法示例：
     - `# 一级标题`
     - `## 二级标题`
     - `### 三级标题`
   - 建议：一篇笔记**只用一个 `#` 作为主标题**，其他用 `## / ###` 作为小节。

### 粗体 / 斜体 / 高亮 / 删除线
   - 粗体：`**重要内容**` → **重要内容**
   - 斜体：`*次要说明*` → *次要说明*
   - 高亮（Obsidian 支持）：`==需要特别注意==` → ==需要特别注意==
   - 删除线：`~~已作废内容~~` → ~~已作废内容~~

### 无序列表 / 有序列表
   - 无序列表：
     - `- 项目 1`
     - `- 项目 2`
   - 有序列表：
     - `1. 步骤一`
     - `2. 步骤二`

### 任务列表（待办清单）
   - 语法：
     - `- [ ] 未完成任务`
     - `- [x] 已完成任务`
   - 适合配合标签使用：`- [ ] 写 Obsidian 教程 #todo`

### 引用块
   - 语法：  
     - `> 这是一个引用，可以用来记录别人的话、文档原文。`

### Callout（标注块）
   - 作用：在笔记里插入带**图标和背景色**的醒目区块，适合做提示、警告、摘要、示例等，比普通引用块更直观。
   - 基本语法：
     - `> [!类型] 标题` 开头，下面每行都以 `> `（`>` 后跟**一个空格**）开头写内容，中间不要有空行；
     - 示例（callout 行首 `>` 前最多 3 个空格，否则无法正确解析）：
> [!tip] 小技巧
> 选中文字后按 `Ctrl + K` 可以快速添加链接。

   - 常用类型（同一行里的多个名字是同一风格的不同写法）：
     - `note`：笔记 / 说明；
     - `abstract` / `summary` / `tldr`：摘要；
     - `info` / `todo`：信息 / 待办；
     - `tip` / `hint` / `important`：提示 / 重要；
     - `success` / `check` / `done`：成功 / 完成；
     - `warning` / `caution` / `attention`：警告；
     - `failure` / `fail` / `missing`：失败 / 缺失；
     - `danger` / `error`：危险 / 错误；
     - `bug`：错误；
     - `example`：示例；
     - `quote` / `cite`：引用。
   - 折叠控制：
     - `> [!info]+ 标题`：默认**展开**；
     - `> [!info]- 标题`：默认**折叠**，点击可展开。
   - 注意：callout 内部**不要有空行**，否则空行下面的内容不会被包进同一块里。

### 行内代码 & 代码块
   - 行内代码：  
     - 语法：`` `使用 [[链接]] 可以创建笔记间的连接` ``  
     - 效果：`使用 [[链接]] 可以创建笔记间的连接`
   - 多行代码块（注意三个反引号 ```）：  
     - 示例（写在笔记里）：
       ```bash
       git status
       git commit -m "msg"
       ```
     - Obsidian 会根据 `bash` 做语法高亮。

### 分割线
   - 语法：单独一行 `---`  
   - 用来在一篇笔记里分隔不同模块。

### 表格
   - 示例：
     ```markdown
     | 日期       | 内容          | 备注     |
     | ---------- | ------------- | -------- |
     | 2026-02-10 | 了解 Obsidian | 初次使用 |
     ```
   - 渲染后就是一个简单表格，适合做小型对比、记录。

你可以把这一节当成“速查表”，写笔记时不记得语法就来这里复制一行改一改即可。

---

## 链接与结构化
1. **页面链接 `[[ ]]`**
   - 输入 `[[` 后开始敲字，会自动联想现有笔记；
   - 如果不存在该名字，直接回车会创建“新笔记”（适合先写后整理）。
2. **块引用与标题引用**
   - `[[笔记名#某个标题]]`：直接跳到那篇笔记的对应标题；
   - `[[笔记名^块ID]]`：可以引用某一具体段落（用得多了适合做“知识卡片”）。
3. **反向链接（Backlinks）**
   - 右侧面板可以看到“哪些笔记链接到了当前这篇笔记”；
   - 适合梳理“这个概念 / 项目，在别的地方哪里出现过？”

---

## 标签与搜索
1. **标签用法**
   - 行内写 `#todo` / `#idea` / `#note/obsidian` 等；
   - 建议用“层级标签”：如 `#project/blueWave`、`#status/doing`。
2. **搜索技巧**
   - `Ctrl + Shift + F`：全库搜索；
   - 语法示例：
     - `tag:#todo`：查所有待办；
     - `"精确词组"`：双引号内为精确匹配；
     - `file:obsidian`：文件名包含 `obsidian` 的笔记。

---

## 文档属性（Frontmatter）
文档属性是写在**笔记最顶部**、用 `---` 包起来的一段 YAML，用来存这篇笔记的**结构化元数据**（标签、日期、自定义字段等），方便检索和配合插件做汇总。

### 怎么写
- 必须在文件**第一行**写 `---`，下面按 `key: value` 或列表写属性，最后再写一行 `---`，后面才是正文。
- 示例：
  ```yaml
  ---
  tags: [obsidian, 技巧]
  aliases: [Obsidian 技巧]
  date: 2026-02-10
  status: 进行中
  ---
  ```
- 添加方式：`Ctrl + ;` 快速插入属性块，或在命令面板里搜「添加文件属性」；也可以直接在笔记开头手打 `---` 再写键值对。

### 常用内置属性
- `tags`：标签（可写多个，如 `[a, b]`），和正文里的 `#tag` 互补，适合在属性里统一管理。
- `aliases`：别名，用 `[[别名]]` 时也能打开这篇笔记。
- `cssclasses`：给这篇笔记挂 CSS 类名，方便用片段做单独样式。

### 自定义属性与类型
- 在「添加文件属性」或属性面板里可以给属性设**类型**：文本、列表、数字、复选框、日期、日期时间等；同一属性名在整个仓库里类型一致，便于后续用 Dataview 等插件按条件查询（如「列出所有 status 为进行中的笔记」）。

### 用好了能做什么
- 用**标签 + 日期 + 自定义状态**把笔记分类，再配合 Dataview 做动态列表、看板、统计；
- 发布时可用属性控制是否发布、摘要等；
- 和「链接、标签」一起用，能既保留灵活写作，又有一份可查询的元数据。

---

## 日记 / 工作流建议
1. **每日笔记（Daily Notes）**
   - 建议开启官方“每日笔记”插件；
   - 模板示例：日期、今日待办、今日记录、复盘。
2. **项目驱动写作**
   - 为每个项目建一个主笔记：`项目-BlueWaveDailyWorking 总览`；
   - 子笔记通过 `[[子任务-xxx]]` 链接起来；
   - 通过标签记录状态：`#status/todo`、`#status/doing`、`#status/done`。

---

## 常用插件推荐（基础向）
> 插件是“按仓库生效”的，你在当前仓库启用后，**其他仓库需要单独再启用一次**。

### Templater / Templates（模板相关）
   - 用于快速插入固定格式内容（如日报、会议记录、代码片段说明等）。

### Calendar（日历）
   - 更直观地查看每日笔记，点击日期即可创建 / 打开当天笔记。

### Tasks / Todo 插件
   - 支持更复杂的任务管理（如带截止日期、重复任务、过滤视图等），适合把全库的 `- [ ]` 任务统一拉出来看。
   - 基本用法（写任务）：
     - 在任意笔记里用标准任务语法：`- [ ] 学习 Obsidian Tasks 插件`；
     - 可以在行尾追加信息：截止日期、重复规则、标签等，例如：  
       - `- [ ] 学习 Tasks 插件 📅 2026-02-12 🔁 every week #todo`。
   - 常用查询示例（在某个“任务总览”笔记里写查询块，下面代码块直接复制到笔记里即可执行）：
     - **列出所有未完成任务**：
       ```tasks
       not done
       ```
     - **列出所有已完成任务**：
       ```tasks
       done
       ```
     - **按“状态标签”过滤（例如只看进行中 ongoing 的任务）**：  
       在任务行里加上状态标签，例如：`- [ ] 学习 Tasks 插件 #status/ongoing`，然后用：
       ```tasks
       not done
       description includes #status/ongoing
       ```
       - 这里利用的是：Tasks 会把行内标签当成任务描述的一部分来过滤。
     - **如果你已经在文档属性里用 `status: ongoing / done` 这类字段**：  
       - 目前 Tasks **不支持** 用 `page.status` 按文档属性筛选任务；  
       - 想基于 frontmatter 做更复杂筛选时，可配合 Dataview，用**行内标签**更稳（见下方 Dataview 小节）。
   - 建议：可以单独建一篇 `任务看板 / task dashboard`，里面放多个 `tasks` 查询块，分别显示：
     - 今天到期、本周到期、所有未完成、进行中项目对应的任务等。

### Advanced Tables（表格编辑增强）
   - 目标：让 Obsidian 里的 Markdown 表格，尽量接近 Emacs Org 表格那种“自动对齐、自动补全、Tab 跳格”的体验。
   - 安装：在 Obsidian 的“社区插件”里搜索 `Advanced Tables`，安装并启用即可。
   - 使用要点（实测结论）：
     - **必须“顶格”写表头**：行首第一个字符就是 `|`，例如 `| 日期 | 内容 | 备注 |`；  
     - 紧跟一行分隔行（例如 `| - | - | - |`，插件会自动补成 `| --- | --- | --- |`）；  
     - 之后的行就会被当作表格，`Tab` 可以在单元格间跳转，并自动补全竖线、对齐列宽。
   - 适用场景：需要经常在笔记里写小表格、记录信息时，用它能显著减少手动对齐的工作量。

### Custom Attachment Location（自定义附件路径）
   - 作用：把所有附件（尤其是图片）按“**每篇笔记一个子目录**”的方式集中管理，避免仓库根目录堆满 `Pasted image xxxx`。
   - 常见设置（按界面一步一步点就行）：
     - `设置 → 选项 → 文件与链接`
       1. `使用 Wiki 链接`：关闭；  
       2. `内部链接类型`：基于当前笔记的相对路径。
     - `设置 → 选项 → 第三方插件 → Custom Attachment Location`
       1. `Markdown URL 格式`：`assets/${noteFileName}/${generatedAttachmentFileName}`；  
       2. `Move/renames`：
          - `附件重命名模式`：全部；  
          - `是否重命名附件文件夹`：是。
   - 图片宽度小技巧（在使用标准 Markdown 图片语法时）：
      - 复制 / 粘贴图片后，通常会得到类似：`![](assets/test/file-20260211171141361.png)`；  
      - 如果图片太宽，可以在中括号里写一个数字作为宽度，例如：  
        - `![400](assets/test/file-20260211171141361.png)`  
        - 这样会按大约 400 像素宽度显示该图片，比铺满整行更适合嵌在正文里。
   - 实际效果：  
     - 每篇笔记对应一个自己的附件文件夹，结构类似：  
       - `笔记A.md`  
       - `assets/笔记A/xxx.png`  
       - `assets/笔记A/yyy.png`  
     - 仓库更干净、可移植性更好，图片和附件也不容易“散落一地找不到”。

### Highlightr（多色高亮）
   - 作用：在笔记里为文字提供**多种颜色的高亮**，比 Obsidian 自带的 `==高亮==` 更丰富，适合区分重点、类型、状态等。
   - 使用方式：
     - 选中要高亮的文字 → 在弹出菜单里点「高亮」→ 选择颜色；
     - 取消高亮：选中已高亮文字 → 菜单里点「取消高亮」；
     - 快捷键设置有两种常用方式：  
       1. `设置 → 选项 → 快捷键`，搜索 `Highlightr`，为「打开高亮菜单」以及各颜色命令绑定快捷键；  
       2. `设置 → 社区插件 → Highlightr`，在插件介绍页点击「快捷键」按钮，会跳转到同一快捷键设置界面。
   - 可选配置：支持在插件设置里自定义高亮颜色（颜色名 + 色值），浅色/深色主题都适用；若把 highlight method 设为 `css-classed`，还可配合 CSS 片段做出仿荧光笔等样式。

### Dataview（查询引擎，进阶）
   - 作用：把整个笔记库当成一个“小数据库”，可以按**文档属性、标签、文件夹、时间**等条件，生成动态的列表 / 表格 / 任务视图。
   - 基本语法概念：
     - 所有查询都写在代码块里，语言标识为 `dataview`，例如：` ```dataview` 开头，下面写查询语句，最后 ` ``` ` 结束。
     - 常见关键字：`list`（列表）、`table`（表格）、`task`（任务）、`from`（数据来源）、`where`（筛选条件）、`sort`（排序）。
   - 常见示例（下面这些代码块直接写在笔记里即可执行）：
     - **按标签列出笔记列表**（比如所有带 `#LTE/PCT` 的笔记）：
       ```dataview
       list from ""
       where contains(tags, "LTE/PCT")
       sort file.ctime desc
       ```
     - **按文档属性 `status` 列出项目笔记**（例如只看 `ongoing` 的项目）：
       ```dataview
       table status, date
       from ""
       where status = "ongoing"
       sort date desc
       ```
     - **按“状态标签”列出任务并分组**（在任务行里加 `#status/ongoing`，然后用 Dataview 查）：
       ```dataview
       task from ""
       where !completed and contains(text, "#status/ongoing")
       group by file.link
       ```
       - 说明：Tasks 和 Dataview 的查询语法是**独立的**，不能混用；用 `file.status = "ongoing"` 按 frontmatter 过滤任务在某些环境下不稳定，建议用行内标签 `#status/ongoing` 配合 `contains(text, ...)`。
   - 使用建议：
     - 先在项目 / 日记笔记里稳定使用好文档属性（例如：`date`、`status`、`area` 等），再用 Dataview 做“总览页”；  
     - 可以为每个领域建一篇 `dashboard`，里面放多个 Dataview 查询块：最近更新、进行中项目、待办任务等。

---

## 同步与备份小贴士
1. **Git 仓库管理**
   - 你现在这个仓库就是一个 Obsidian 仓库，很适合用 Git 做版本管理；
   - 注意 `.obsidian` 是否纳入版本控制：  
     - 包含：不同机器能同步插件设置；  
     - 不包含：不同机器可以保持“轻量”设置，各自独立。
2. **多设备使用**
   - 保持“仓库路径 + 同步工具”一致即可；
   - 每台设备的 Obsidian 都要“打开同一个文件夹”作为 Vault。

---

## 建议的使用习惯总结
1. 一切内容落到 **本地 Markdown 文件**，不怕平台锁死。
2. 尝试用 `[[链接]]` 和标签，把知识串起来，而不是只用“层级目录”。
3. 定期整理：  
   - 把临时的“收集笔记”拆分成小的概念 / 卡片笔记；  
   - 通过链接和标签建立结构。

你可以先按照 **“每日笔记 + 项目主笔记 + 链接 / 标签”** 这个最小工作流用起来，后面我可以根据你的实际使用情况再一起优化结构和插件配置。